_Please, complete the following task:_ 

**Task 1** Based on module 4, define all the external dependencies (as an example: message broker, SQL, or NoSQL database) and containerize them (use the pre-build container images). Catalog and Cart services should be reworked/reconfigured to work with the containerized versions of the dependencies. 

**Task 2** Create a docker-compose file for all the services (Catalog and Cart) and external dependencies from Task1. Build, run, and test the compose file.

Note: _in the docker files, you should be able to explain all the steps (commands) to your mentor._

> **NB!** Scoreboard:

* 1-79 - A mentee can lead a discussion on the “Questions to Discuss” topics.
* 80-100 - Task has been completed.