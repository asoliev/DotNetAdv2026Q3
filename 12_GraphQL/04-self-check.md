_Questions for the self-check:_

1. What is GraphQL? What benefits does it provide compared to other types of API? What are the drawbacks?
2. What implementations of GraphQL servers exist for .NET? What is the difference between them?
3. For which types of projects would you suggest using GraphQL as an API and for which you would not?
4. Does GraphQL support versioning and caching?
5. How is performance affected by the GraphQL API? What is 1+N problem?